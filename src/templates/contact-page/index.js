import { Layout } from "components/common";
import React from "react";

const ContactPage = () => {
  return <Layout title="Contact">This is the contact Page</Layout>;
};

export default ContactPage;
